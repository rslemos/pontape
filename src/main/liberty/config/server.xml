<server>

    <featureManager>
        <platform>microprofile-4.0</platform>
        <feature>mpgraphql</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="9080"
                  httpsPort="9443" />

</server>
